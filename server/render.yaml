services:
  - type: web
    name: chess-relay
    env: python
    region: singapore
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python relay_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
